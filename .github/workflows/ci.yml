name: CI

on:
  push:
    branches: [ "main", "develop" ]
  pull_request:
    branches: [ "main", "develop" ]

jobs:
  build-test:
    runs-on: ubuntu-latest

    env:
      LLM_BASE_URL: ${{ secrets.LLM_BASE_URL }}
      LLM_API_KEY: ${{ secrets.LLM_API_KEY }}
      LLM_MODEL: ${{ secrets.LLM_MODEL }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
          cache: maven

      - name: Build (skip tests)
        run: mvn -B -DskipTests clean package

      - name: Generate tests & AI report from story
        run: |
          mkdir -p reports
          java -cp target/*.jar com.example.tools.StoryReportGenerator \
            --storyFile=stories/ATT-123.yaml \
            --outputFile=reports/ATT-123-ai-report.md

      - name: Upload AI story report
        uses: actions/upload-artifact@v4
        with:
          name: ATT-123-ai-report
          path: reports/ATT-123-ai-report.md

      - name: Run tests (includes AI-generated tests)
        run: mvn -B test

      - name: Print custom test summary (ACCEPTED / REJECTED)
        run: |
          java -cp target/*.jar com.example.tools.TestResultSummaryGenerator

      - name: Upload JUnit reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: junit-report
          path: target/surefire-reports
